# BoxOfficeRevenuePrediction
End-to-end Movie Revenue Prediction using ML models. Includes preprocessing, feature engineering, model training (RF, GBR, XGBoost), and evaluation. Uses log-transformed revenue with back-transformed predictions and feature importance analysis.
